DECLARE @MyTran varchar(20) ; SET @MyTran = 'pjn01'
/*
SELECT NUMBER,PLANNED_END,CNF_PLANNED_DAYS,CNF_PLANNED_OVERDUE
  FROM CM3RM1
  WHERE CNF_PLANNED_DAYS IS NULL
  WHERE CNF_PLANNED_DAYS='0'
  WHERE CNF_PLANNED_OVERDUE IS NULL
  WHERE CNF_PLANNED_OVERDUE IS NOT NULL
  WHERE [OPEN] = 't' AND CNF_PLANNED_OVERDUE = 'f'

UPDATE CM3RM1 
  SET CNF_PLANNED_OVERDUE = 'f'
  WHERE CNF_PLANNED_OVERDUE IS NULL
  WHERE [OPEN] = 't' AND CNF_PLANNED_OVERDUE = 'f'

BEGIN TRAN @MyTran
ROLLBACK TRAN @MyTran
COMMIT TRAN @MyTran
*/

SELECT NUMBER,PLANNED_END,CNF_PLANNED_DAYS,CNF_PLANNED_OVERDUE
  FROM CM3RM1
  WHERE [OPEN] = 't' AND CNF_PLANNED_OVERDUE = 'f'
